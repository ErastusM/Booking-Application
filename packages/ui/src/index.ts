// @bookplus/ui — shared component library for the customer + business apps.
//
// The original BrandMark lived here but was retired: both apps now render the
// brand via their own <Wordmark> (book·plus + smile / underline). This package
// is intentionally kept as the home for the NEXT shared component; add exports
// here as components are extracted. `export {}` keeps it a valid ES module.
export {};
