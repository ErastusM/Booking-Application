# API origin only — the api-client appends /api itself.
VITE_API_URL=http://localhost:5050
# Business app origin — cross-app links ("Become a provider", business login/redirects) point here.
VITE_BUSINESS_URL=http://localhost:3003
